# qulaynarx

### How to run 

```
git clone https://codeberg.org/iskandar/qulaynarx.git
cd qulaynarx
python -m venv venv
source venv/Scripts/activate
pip install scrapy scrapy-playwright
playwright install
cd markets
scrapy crawl korzinka
```